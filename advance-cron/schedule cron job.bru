meta {
  name: schedule cron job
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/api/cron
  body: json
  auth: none
}

body:json {
  {
    "name": "every 2 min",
    "cronExpression": "*/2 * * * *",
    "webhookUrl": "https://httpbin.org/post",
    "isActive": true
  }
}
